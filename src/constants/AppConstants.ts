export enum PLATFORM_OS_TYPE {
  IOS = 'ios',
  ANDROID = 'android',
}

export enum SETTINGS_KEY {
  THEME = 'theme',
  LANGUAGE = 'language',
}

export enum BIRTHDAY_TABS {
  LIST = 'List View',
  CALENDAR = 'Calendar View',
}
